{
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
{
  "name": "react-deploy",
  "version": "0.1.0",
  "homepage": "https://github.com/DimiTanof/Lession4.git", // Home Page
  "private": true,
  "dependencies": {
    ...
  },
  ...
  "devDependencies": {
    "gh-pages": "^3.2.3" // gh-pages plugin
  }
}
{
	"scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build", // Build before deploy
    "deploy": "gh-pages -d build" // Deploy the "build" folder
  },
}